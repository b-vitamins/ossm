device: cpu
max_steps: 100000
epochs: null
log_interval: 1000
eval_interval: 5000
grad_clip: 1.0
classification: false
metric: mse
checkpoint_dir: ${paths.work_dir}
prefetch_gpu: false
prefetch_depth: 2
cudnn_benchmark: false
